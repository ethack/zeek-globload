[package]
script_dir = scripts
build_command = ./configure && cd build && make
test_command = cd testing && btest -c btest.cfg
summary = TODO: A summary of GlobLoad in one line
description = TODO: A more detailed description of GlobLoad.
	It can span multiple lines, with this indentation.

[template]
source = package-template
version = master
zkg_version = 2.9.0-1
features = github-ci,plugin

[template_vars]
name = GlobLoad
namespace = Corelight

